apiVersion: v1
kind: Service
metadata:
  name: transaction
  namespace: pointofsale
spec:
  selector:
    app: transaction
  ports:
    - name: grpc
      port: 9009
      targetPort: 9009
  type: ClusterIP
