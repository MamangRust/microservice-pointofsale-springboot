apiVersion: v1
kind: Service
metadata:
  name: merchant
  namespace: pointofsale
spec:
  selector:
    app: merchant
  ports:
    - name: grpc
      port: 9005
      targetPort: 9005
  type: ClusterIP
