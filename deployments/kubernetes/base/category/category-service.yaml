apiVersion: v1
kind: Service
metadata:
  name: category
  namespace: pointofsale
spec:
  selector:
    app: category
  ports:
    - name: grpc
      port: 9015
      targetPort: 9015
  type: ClusterIP
